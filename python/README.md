# Boost.Python

Reflect a CAF Graph.

## Build

To build on Windows:

```
mkdir build
cd build
cmake -G "Visual Studio 15 2017 Win64" ..
cmake --build .
```

On Linux:

```
mkdir build
cd build
cmake ..
cmake --build .
```
